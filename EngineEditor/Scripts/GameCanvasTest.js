// const canvas = document.getElementById("engineCanvas");
// const context = canvas.getContext('2d');
//
// canvas.width = window.innerWidth;
// canvas.height = window.innerHeight;
//
// const canvasCenterX = canvasOverlay.offsetWidth/2 + canvasOverlay.getBoundingClientRect().x;
// const canvasCenterY = canvasOverlay.offsetHeight/2 + canvasOverlay.getBoundingClientRect().y;
//
// document.addEventListener("mousemove", function() {
//     context.clearRect(0,0, canvas.width, canvas.height);
//
//     context.beginPath();
//     context.fillStyle = 'red';
//     context.arc(canvasCenterX + canvasOffsetX, canvasCenterY + canvasOffsetY, 500, 0, 2 * Math.PI);
//     context.stroke();
//     context.fill();
//
//     context.closePath();
// })
//
// context.clearRect(0,0, canvas.width, canvas.height);
//
// context.beginPath();
// context.fillStyle = 'red';
// context.arc(canvasCenterX + canvasOffsetX, canvasCenterY + canvasOffsetY, 500, 0, 2 * Math.PI);
// context.stroke();
// context.fill();
//
// context.closePath();